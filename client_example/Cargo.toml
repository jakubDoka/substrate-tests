[package]
name = "client_example"
version = "0.1.0"
edition = "2021"

[dependencies]
subxt = "0.32.0"
subxt-signer = { features = ["subxt"] }
tokio = { features = ["rt-multi-thread", "macros", "time"] }

[workspace]
